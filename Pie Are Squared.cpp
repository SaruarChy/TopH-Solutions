#include <bits/stdc++.h>
using namespace std;

const double PI = acos(-1.0);
int main(void)
{
    double r;
    cin >> r;
    cout << fixed << setprecision(10) << PI*r*r << endl;

    return 0;
}
